{
  "name": "expenseflow-monorepo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "dev:frontend": "turbo run dev --filter expenseflow-frontend",
    "dev:backend": "turbo run dev --filter expenseflow-backend",
    "build:frontend": "turbo run build --filter expenseflow-frontend",
    "build:backend": "turbo run build --filter expenseflow-backend",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "packageManager": "pnpm@9.1.0"
}
